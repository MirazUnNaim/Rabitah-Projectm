[build]
builder = "DOCKERFILE"
dockerfilePath = "Rabitah-Backend/Dockerfile"
watchPatterns = ["Rabitah-Backend/**", "pom.xml"]

[deploy]
healthcheckPath = "/actuator/health"
healthcheckTimeout = 120
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 5
